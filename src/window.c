// window.c (ชั้น platform) - ยังเป็นโครงเปล่า เดี๋ยว step 1 ค่อยเติมของจริง
#include "window.h"

HWND window_create(HINSTANCE hInstance, int nCmdShow)
{
    (void)hInstance;
    (void)nCmdShow;
    return NULL;   /* ยังไม่ทำอะไร */
}

int window_run(void)
{
    return 0;      /* ยังไม่ทำอะไร */
}
